meta:
  environment: (( merge ))
  dns_root: (( merge ))
  security_groups: (( merge ))
  persistent_disk: 4096

  stemcell:
    name: bosh-aws-xen-ubuntu
    version: latest

jobs:
  - name: bosh_pipeline_dashboard_agent_leader_z1
    instances: 1
    networks:
      - name: bosh_pipeline_dashboard_agent1
    persistent_disk: (( meta.persistent_disk ))
  - name: bosh_pipeline_dashboard_agent_z1
    instances: 2
    networks:
      - name: bosh_pipeline_dashboard_agent1
    persistent_disk: (( meta.persistent_disk ))
    properties:
      bosh_pipeline_dashboard_agent:
        leader_address: (( "0.bosh-pipeline-dashboard-agent-leader-z1.bosh-pipeline-dashboard-agent1." meta.environment "." meta.dns_root ))

compilation:
  cloud_properties:
    instance_type: m1.small

resource_pools:
  - name: small_z1
    cloud_properties:
      instance_type: m1.small

networks:
  - name: floating
    type: vip
    cloud_properties: {}
  - name: bosh_pipeline_dashboard_agent1
    type: dynamic
    cloud_properties:
      security_groups: (( meta.security_groups ))
